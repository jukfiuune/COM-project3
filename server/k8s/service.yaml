apiVersion: v1
kind: Service
metadata:
  name: cleanmap-api-service
  labels:
    app: cleanmap-api
spec:
  selector:
    app: cleanmap-api
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
      nodePort: 30543
  type: NodePort
