apiVersion: v1
kind: Secret
metadata:
  name: nexora-secret
  labels:
    app: nexora-api
type: Opaque
data:
  # Generate with:
  # [Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes("your-connection-string"))
  # or: echo -n "your-connection-string" | base64
  mongodb-connection-string: REPLACE_WITH_BASE64_ENCODED_CONNECTION_STRING
